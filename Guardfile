guard 'livereload' do
  watch(%r{web/templates/layout/.+\.(eex)$})
  watch(%r{web/templates/page/.+\.(eex)$})
  watch(%r{web/templates/layout/.+\.(eex)$})
  watch(%r{web/views/.+\.(ex)$})
  watch(%r{priv/static/css/.+\.(css)$})
end
